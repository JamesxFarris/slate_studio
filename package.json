{
  "name": "slate-studio",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-agent job application research and prep system",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck"
  }
}
